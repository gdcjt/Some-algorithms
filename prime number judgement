bool isprime(int N) {

    if(N == 2) return true;

    if(N % 2 == 0) return false;
    
    double sqr = sqrt(N);
    
    for (int i = 2;i <= sqr;i++) if(N % i == 0) return false;
    
    return true;
    
}
